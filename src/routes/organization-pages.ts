import { Hono } from "hono";
import type { User } from "better-auth/types";

const app = new Hono<{ Variables: { user?: User } }>();

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => ({
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#039;",
  })[character] ?? character);
}

function jsonState(value: unknown): string {
  return JSON.stringify(value).replace(/</g, "\\u003c");
}

function renderOrganizationPage(user: User, organizationId?: string): string {
  const state = jsonState({ organizationId: organizationId ?? "" });
  return `<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>${organizationId ? "Organization settings" : "Organizations"} — Rendro</title><style>
:root{color-scheme:light;--page:#fafafa;--surface:#fff;--surface-2:#f4f4f5;--text:#18181b;--muted:#71717a;--border:#e4e4e7;--accent:#f97316;--accent-hover:#fdba74;--danger:#b91c1c;--success:#15803d;--focus:rgba(249,115,22,.2);--shadow:0 18px 48px rgba(24,24,27,.07)}html.dark{color-scheme:dark;--page:#09090b;--surface:#18181b;--surface-2:#27272a;--text:#fafafa;--muted:#a1a1aa;--border:#3f3f46;--accent:#fb923c;--accent-hover:#fdba74;--danger:#fca5a5;--success:#86efac;--focus:rgba(251,146,60,.22);--shadow:0 18px 48px rgba(0,0,0,.35)}*{box-sizing:border-box}body{margin:0;background:var(--page);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;-webkit-font-smoothing:antialiased}button,input,select{font:inherit}a{color:inherit;text-underline-offset:3px}.topbar{height:64px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;padding:0 clamp(20px,4vw,52px);background:var(--surface)}.brand{font-weight:760;letter-spacing:-.04em;text-decoration:none;font-size:20px}.brand i{color:var(--accent);font-style:normal}.top-actions{display:flex;align-items:center;gap:16px;font-size:12px;color:var(--muted)}.top-actions a{font-weight:650}.theme{width:34px;height:34px;border:1px solid var(--border);border-radius:50%;background:var(--surface);color:var(--muted);cursor:pointer}.page{width:min(1120px,calc(100% - 32px));margin:0 auto;padding:52px 0 72px}.page-head{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-bottom:30px}.page-head h1{font-size:34px;letter-spacing:-.04em;margin:4px 0 7px}.page-head p{color:var(--muted);font-size:14px;margin:0}.eyebrow{color:var(--accent)!important;text-transform:uppercase;letter-spacing:.12em;font-size:11px!important;font-weight:750}.button{min-height:40px;border:1px solid var(--border);border-radius:8px;background:var(--surface);color:var(--text);padding:9px 14px;font-weight:650;font-size:13px;cursor:pointer;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;gap:7px}.button:hover{background:var(--surface-2)}.button.primary{background:var(--accent);border-color:var(--accent);color:#18181b}.button.primary:hover{background:var(--accent-hover)}.button.danger{color:var(--danger)}.button:disabled{opacity:.55;cursor:wait}.grid{display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:24px}.panel{background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow)}.panel-head{padding:20px 22px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;gap:12px}.panel-head h2,.form-panel h2{font-size:16px;margin:0;letter-spacing:-.015em}.panel-head p{color:var(--muted);font-size:11px;margin:4px 0 0}.org-list{display:grid}.org-card{display:grid;grid-template-columns:42px minmax(0,1fr) auto;align-items:center;gap:14px;padding:18px 22px;border-bottom:1px solid var(--border);cursor:pointer}.org-card:last-child{border-bottom:0}.org-card:hover{background:var(--surface-2)}.org-icon{width:42px;height:42px;border-radius:9px;background:color-mix(in srgb,var(--accent) 13%,var(--surface));color:var(--accent);display:grid;place-items:center;font-weight:800;text-transform:uppercase}.org-copy strong{display:block;font-size:14px}.org-copy span{display:block;color:var(--muted);font-size:11px;margin-top:4px}.arrow{color:var(--muted)}.empty{padding:48px 24px;text-align:center;color:var(--muted);font-size:13px}.form-panel{padding:22px;align-self:start}.form-panel>p,.section-copy{font-size:12px;line-height:1.55;color:var(--muted);margin:7px 0 20px}.field{display:block;margin-bottom:15px}.field>span{display:block;font-size:11px;font-weight:650;margin-bottom:6px}.field input,.field select,select.inline{width:100%;height:40px;border:1px solid var(--border);border-radius:7px;background:var(--surface);color:var(--text);padding:0 10px;outline:none}.field input:focus,.field select:focus,select.inline:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--focus)}.field .hint{color:var(--muted);font-weight:400}.form-panel .button{width:100%}.message{display:none;margin:12px 0 0;color:var(--danger);font-size:11px;line-height:1.5}.message:not(:empty){display:block}.message.success{color:var(--success)}.back{display:inline-flex;color:var(--muted);font-size:12px;font-weight:650;text-decoration:none;margin-bottom:24px}.tabs{display:flex;border-bottom:1px solid var(--border);padding:0 22px;gap:24px}.tab{background:none;border:0;color:var(--muted);font-weight:650;font-size:12px;padding:15px 0 13px;border-bottom:2px solid transparent;cursor:pointer}.tab.active{color:var(--text);border-bottom-color:var(--accent)}.tab-panel{padding:22px}.tab-panel[hidden]{display:none}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px}.stat{border:1px solid var(--border);border-radius:9px;padding:16px}.stat strong{font-size:23px;display:block}.stat span{color:var(--muted);font-size:11px}.split{display:grid;grid-template-columns:minmax(0,1fr) 290px;gap:22px}.table{border:1px solid var(--border);border-radius:9px;overflow:hidden}.row{display:grid;grid-template-columns:minmax(0,1fr) 110px 110px;gap:12px;align-items:center;padding:13px 15px;border-bottom:1px solid var(--border);font-size:12px}.row:last-child{border-bottom:0}.person strong{display:block;font-size:12px}.person span{color:var(--muted);font-size:10px;display:block;margin-top:2px}.role{color:var(--muted);text-transform:capitalize}.row select.inline{height:32px;font-size:11px}.subhead{font-size:13px;margin:24px 0 10px}.pending{color:#a16207;font-size:10px;text-transform:uppercase;letter-spacing:.08em}.team-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.team{border:1px solid var(--border);border-radius:9px;padding:16px}.team strong{font-size:13px}.team p{color:var(--muted);font-size:11px;margin:5px 0 14px}.team-actions{display:flex;gap:7px}.team-actions select{height:34px;min-width:0;flex:1;border:1px solid var(--border);border-radius:6px;background:var(--surface);color:var(--text);font-size:11px;padding:0 7px}.team-actions button{height:34px;border:1px solid var(--border);border-radius:6px;background:var(--surface);color:var(--text);font-size:11px;font-weight:650;cursor:pointer}.loading{padding:56px;text-align:center;color:var(--muted);font-size:12px}.account-note{font-size:11px;color:var(--muted)}
@media(max-width:800px){.grid,.split{grid-template-columns:1fr}.form-panel{order:-1}.page{padding-top:30px}.page-head{align-items:flex-start;flex-direction:column}.top-actions span{display:none}.stats{grid-template-columns:1fr}.team-grid{grid-template-columns:1fr}.row{grid-template-columns:minmax(0,1fr) 90px}.row>*:last-child{grid-column:1/-1}}@media(prefers-reduced-motion:reduce){*{transition-duration:.01ms!important;animation-duration:.01ms!important}}
</style></head><body><header class="topbar"><a class="brand" href="/">Rendro<i>.</i></a><div class="top-actions"><span>${escapeHtml(user.email)}</span><a href="/account/security">Security</a><a href="/api/auth/sign-out">Sign out</a><button class="theme" id="theme-toggle" aria-label="Switch color theme">◐</button></div></header><main class="page" id="app"><div class="loading">Loading organizations…</div></main><script>window.__ORG_STATE__=${state};</script><script>
(function(){"use strict";var state=window.__ORG_STATE__;var root=document.documentElement;var media=matchMedia("(prefers-color-scheme: dark)");function currentTheme(){var saved=localStorage.getItem("commentor-theme");return saved==="dark"||saved==="light"?saved:(media.matches?"dark":"light");}function applyTheme(){root.classList.toggle("dark",currentTheme()==="dark");}applyTheme();media.addEventListener("change",applyTheme);document.getElementById("theme-toggle").addEventListener("click",function(){localStorage.setItem("commentor-theme",currentTheme()==="dark"?"light":"dark");applyTheme();});
var mount=document.getElementById("app");function el(tag,className,text){var node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node;}async function request(path,options){var response=await fetch(path,Object.assign({headers:{Accept:"application/json","Content-Type":"application/json"}},options||{}));var data=null;try{data=await response.json();}catch(_error){}if(!response.ok){var message=data&&(data.message||data.error);throw new Error(typeof message==="string"?message:"Request failed. Please try again.");}return data;}function post(path,body){return request(path,{method:"POST",body:JSON.stringify(body)});}function slugify(value){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,48);}
async function renderList(){var organizations=await request("/api/auth/organization/list");mount.innerHTML='<div class="page-head"><div><p class="eyebrow">Workspace</p><h1>Your organizations</h1><p>Select an organization or create a new one.</p></div><a class="button" href="/account/security">Account security</a></div><div class="grid"><section class="panel"><div class="panel-head"><div><h2>Organizations</h2><p>Membership, projects, and documents stay isolated.</p></div><span class="account-note">'+organizations.length+' total</span></div><div class="org-list" id="org-list"></div></section><aside class="panel form-panel"><h2>Create organization</h2><p>Give the workspace a stable name and URL slug. You become its owner.</p><form id="create-org"><label class="field"><span>Name</span><input name="name" autocomplete="organization" required maxlength="80"></label><label class="field"><span>Slug <span class="hint">letters, numbers, hyphens</span></span><input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" maxlength="48"></label><button class="button primary" type="submit">Create organization</button><p class="message" id="create-error"></p></form></aside></div>';var list=document.getElementById("org-list");if(!organizations.length){list.appendChild(el("div","empty","No organizations yet. Create your first workspace."));}organizations.forEach(function(org){var card=el("div","org-card");card.tabIndex=0;card.setAttribute("role","link");var icon=el("span","org-icon",org.name.slice(0,2));var copy=el("span","org-copy");copy.append(el("strong","",org.name),el("span","",org.slug));card.append(icon,copy,el("span","arrow","→"));async function open(){card.style.opacity=".6";await post("/api/auth/organization/set-active",{organizationId:org.id});location.assign("/organizations/"+encodeURIComponent(org.id));}card.addEventListener("click",function(){open().catch(function(error){card.style.opacity="1";alert(error.message);});});card.addEventListener("keydown",function(event){if(event.key==="Enter")card.click();});list.appendChild(card);});var form=document.getElementById("create-org");var name=form.elements.namedItem("name");var slug=form.elements.namedItem("slug");var slugTouched=false;slug.addEventListener("input",function(){slugTouched=true;});name.addEventListener("input",function(){if(!slugTouched)slug.value=slugify(name.value);});form.addEventListener("submit",async function(event){event.preventDefault();var button=form.querySelector("button");var error=document.getElementById("create-error");button.disabled=true;error.textContent="";try{var org=await post("/api/auth/organization/create",{name:name.value.trim(),slug:slug.value.trim(),keepCurrentActiveOrganization:false});location.assign("/organizations/"+encodeURIComponent(org.id));}catch(reason){button.disabled=false;error.textContent=reason.message;}});}
async function renderOrganization(){var orgId=state.organizationId;await post("/api/auth/organization/set-active",{organizationId:orgId});var query="?organizationId="+encodeURIComponent(orgId);var results=await Promise.all([request("/api/auth/organization/get-full-organization"+query),request("/api/auth/organization/list-invitations"+query)]);var org=results[0],invitations=results[1]||[];if(!org)throw new Error("Organization not found.");var members=org.members||[];var teams=org.teams||[];mount.innerHTML='<a class="back" href="/organizations">← All organizations</a><div class="page-head"><div><p class="eyebrow">Organization</p><h1 id="org-name"></h1><p id="org-slug"></p></div><a class="button primary" href="/">Open workspace</a></div><section class="panel"><nav class="tabs" aria-label="Organization sections"><button class="tab active" data-tab="overview">Overview</button><button class="tab" data-tab="members">Members</button><button class="tab" data-tab="teams">Teams</button></nav><div class="tab-panel" id="overview-panel"><div class="stats"><div class="stat"><strong>'+members.length+'</strong><span>Members</span></div><div class="stat"><strong>'+teams.length+'</strong><span>Teams</span></div><div class="stat"><strong>'+invitations.filter(function(i){return i.status==="pending";}).length+'</strong><span>Pending invitations</span></div></div></div><div class="tab-panel" id="members-panel" hidden><div class="split"><div><div class="table" id="member-list"></div><h3 class="subhead">Pending invitations</h3><div class="table" id="invitation-list"></div></div><aside class="form-panel panel"><h2>Invite member</h2><p>Invitations are bound to the exact email address and expire in seven days.</p><form id="invite-form"><label class="field"><span>Email</span><input type="email" name="email" required autocomplete="off"></label><label class="field"><span>Role</span><select name="role"><option value="member">Member</option><option value="admin">Admin</option></select></label><label class="field"><span>Team <span class="hint">optional</span></span><select name="teamId"><option value="">No team</option></select></label><button class="button primary" type="submit">Send invitation</button><p class="message" id="invite-error"></p><p class="message success" id="invite-success"></p></form></aside></div></div><div class="tab-panel" id="teams-panel" hidden><div class="split"><div class="team-grid" id="team-list"></div><aside class="panel form-panel"><h2>Create team</h2><p>Teams organize members now and can scope project access later.</p><form id="team-form"><label class="field"><span>Team name</span><input name="name" required maxlength="80"></label><button class="button primary" type="submit">Create team</button><p class="message" id="team-error"></p></form></aside></div></div></section>';document.getElementById("org-name").textContent=org.name;document.getElementById("org-slug").textContent=org.slug;document.querySelectorAll(".tab").forEach(function(tab){tab.addEventListener("click",function(){document.querySelectorAll(".tab").forEach(function(other){other.classList.toggle("active",other===tab);});["overview","members","teams"].forEach(function(name){document.getElementById(name+"-panel").hidden=tab.dataset.tab!==name;});});});renderMembers(members,teams,orgId);renderInvitations(invitations);renderTeams(teams,members,orgId);var inviteForm=document.getElementById("invite-form");var teamSelect=inviteForm.elements.namedItem("teamId");teams.forEach(function(team){var option=el("option","",team.name);option.value=team.id;teamSelect.appendChild(option);});inviteForm.addEventListener("submit",async function(event){event.preventDefault();var button=inviteForm.querySelector("button");var error=document.getElementById("invite-error");var success=document.getElementById("invite-success");button.disabled=true;error.textContent="";success.textContent="";try{var teamId=teamSelect.value;await post("/api/auth/organization/invite-member",{email:inviteForm.elements.namedItem("email").value.trim(),role:inviteForm.elements.namedItem("role").value,organizationId:orgId,teamId:teamId||undefined});success.textContent="Invitation sent.";inviteForm.reset();}catch(reason){error.textContent=reason.message;}button.disabled=false;});var teamForm=document.getElementById("team-form");teamForm.addEventListener("submit",async function(event){event.preventDefault();var button=teamForm.querySelector("button");button.disabled=true;try{await post("/api/auth/organization/create-team",{name:teamForm.elements.namedItem("name").value.trim(),organizationId:orgId});location.reload();}catch(reason){document.getElementById("team-error").textContent=reason.message;button.disabled=false;}});}
function renderMembers(members,teams,orgId){var list=document.getElementById("member-list");if(!members.length){list.append(el("div","empty","No members found."));return;}members.forEach(function(member){var row=el("div","row");var person=el("span","person");person.append(el("strong","",member.user&&member.user.name||"Member"),el("span","",member.user&&member.user.email||member.userId));row.append(person,el("span","role",member.role));var select=el("select","inline");["member","admin","owner"].forEach(function(role){var option=el("option","",role);option.value=role;option.selected=member.role.split(",").indexOf(role)>=0;select.appendChild(option);});select.setAttribute("aria-label","Change role for "+(member.user&&member.user.email||"member"));select.addEventListener("change",async function(){select.disabled=true;try{await post("/api/auth/organization/update-member-role",{memberId:member.id,role:select.value,organizationId:orgId});}catch(error){alert(error.message);location.reload();}select.disabled=false;});row.append(select);list.appendChild(row);});}
function renderInvitations(invitations){var list=document.getElementById("invitation-list");var pending=invitations.filter(function(invitation){return invitation.status==="pending";});if(!pending.length){list.append(el("div","empty","No pending invitations."));return;}pending.forEach(function(invitation){var row=el("div","row");var person=el("span","person");person.append(el("strong","",invitation.email),el("span","",invitation.role||"member"));row.append(person,el("span","pending","Pending"));var cancel=el("button","button danger","Cancel");cancel.addEventListener("click",async function(){cancel.disabled=true;try{await post("/api/auth/organization/cancel-invitation",{invitationId:invitation.id});row.remove();}catch(error){cancel.disabled=false;alert(error.message);}});row.append(cancel);list.appendChild(row);});}
function renderTeams(teams,members,orgId){var list=document.getElementById("team-list");if(!teams.length){list.append(el("div","empty","No teams yet."));return;}teams.forEach(function(team){var card=el("article","team");card.append(el("strong","",team.name),el("p","","Assign any organization member to this team."));var actions=el("div","team-actions");var select=el("select");var placeholder=el("option","","Choose member…");placeholder.value="";select.appendChild(placeholder);members.forEach(function(member){var option=el("option","",member.user&&member.user.name||member.userId);option.value=member.userId;select.appendChild(option);});var add=el("button","","Add");add.addEventListener("click",async function(){if(!select.value)return;add.disabled=true;try{await post("/api/auth/organization/add-team-member",{teamId:team.id,userId:select.value,organizationId:orgId});add.textContent="Added";}catch(error){add.disabled=false;alert(error.message);}});actions.append(select,add);card.append(actions);list.appendChild(card);});}
(state.organizationId?renderOrganization():renderList()).then(function(){
  if(!state.organizationId)return;
  var projectLink=mount.querySelector(".page-head .button.primary");
  if(projectLink){
    projectLink.href="/organizations/"+encodeURIComponent(state.organizationId)+"/projects";
    projectLink.textContent="Manage projects";
    var keyLink=document.createElement("a");
    keyLink.className="button";
    keyLink.href="/organizations/"+encodeURIComponent(state.organizationId)+"/api-keys";
    keyLink.textContent="API keys";
    projectLink.before(keyLink);
  }
}).catch(function(error){mount.innerHTML='<div class="panel empty"><strong>Unable to load organizations.</strong><p></p><a class="button" href="/organizations">Try again</a></div>';mount.querySelector("p").textContent=error.message;});})();
</script></body></html>`;
}

function renderInvitationPage(user: User, invitationId: string): string {
  const state = jsonState({ invitationId });
  return `<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Organization invitation — Rendro</title><style>:root{color-scheme:light;--page:#fafafa;--surface:#fff;--text:#18181b;--muted:#71717a;--border:#e4e4e7;--accent:#f97316;--danger:#b91c1c}*{box-sizing:border-box}body{margin:0;min-height:100vh;display:grid;place-items:center;padding:20px;background:var(--page);color:var(--text);font-family:Inter,ui-sans-serif,system-ui}.card{width:min(100%,460px);background:var(--surface);border:1px solid var(--border);border-radius:14px;padding:34px;box-shadow:0 20px 55px rgba(24,24,27,.09)}.brand{font-weight:800;letter-spacing:-.04em;margin-bottom:38px}.brand i{color:var(--accent);font-style:normal}.eyebrow{color:var(--accent);font-size:11px;text-transform:uppercase;letter-spacing:.12em;font-weight:750}h1{font-size:28px;letter-spacing:-.035em;margin:7px 0 10px}p{color:var(--muted);font-size:13px;line-height:1.55}.detail{border:1px solid var(--border);border-radius:9px;padding:15px;margin:22px 0}.detail strong,.detail span{display:block}.detail span{font-size:11px;color:var(--muted);margin-top:4px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px}.button{height:42px;border:1px solid var(--border);border-radius:8px;background:var(--surface);color:var(--text);font-weight:650;cursor:pointer}.button.primary{background:var(--accent);border-color:var(--accent);color:#18181b}.button:disabled{opacity:.6}.error{color:var(--danger);font-size:11px}</style></head><body><main class="card"><div class="brand">Rendro<i>.</i></div><div id="content"><p class="eyebrow">Organization invitation</p><h1>Review your invitation</h1><p>Signed in as ${escapeHtml(user.email)}.</p><div class="detail" id="detail">Loading invitation…</div><p class="error" id="error"></p><div class="actions"><button class="button" id="reject">Decline</button><button class="button primary" id="accept">Accept invitation</button></div></div></main><script>window.__INVITATION_STATE__=${state};</script><script>(function(){var id=window.__INVITATION_STATE__.invitationId;var detail=document.getElementById("detail"),error=document.getElementById("error"),accept=document.getElementById("accept"),reject=document.getElementById("reject");async function request(path,options){var response=await fetch(path,Object.assign({headers:{Accept:"application/json","Content-Type":"application/json"}},options||{}));var data=null;try{data=await response.json();}catch(_error){}if(!response.ok)throw new Error(data&&(data.message||data.error)||"Request failed.");return data;}request("/api/auth/organization/get-invitation?id="+encodeURIComponent(id)).then(function(invitation){detail.innerHTML="";var name=document.createElement("strong");name.textContent=invitation.organizationName||"Rendro organization";var role=document.createElement("span");role.textContent="Role: "+(invitation.role||"member");detail.append(name,role);}).catch(function(reason){error.textContent=reason.message;accept.disabled=true;reject.disabled=true;});accept.addEventListener("click",function(){accept.disabled=true;request("/api/auth/organization/accept-invitation",{method:"POST",body:JSON.stringify({invitationId:id})}).then(function(){location.assign("/organizations");}).catch(function(reason){accept.disabled=false;error.textContent=reason.message;});});reject.addEventListener("click",function(){reject.disabled=true;request("/api/auth/organization/reject-invitation",{method:"POST",body:JSON.stringify({invitationId:id})}).then(function(){location.assign("/organizations");}).catch(function(reason){reject.disabled=false;error.textContent=reason.message;});});})();</script></body></html>`;
}

app.get("/organizations", (c) => {
  const user = c.get("user");
  if (!user) return c.redirect("/sign-in?returnTo=%2Forganizations");
  return c.html(renderOrganizationPage(user));
});
app.get("/organizations/:organizationId", (c) => {
  const user = c.get("user");
  const returnTo = `/organizations/${encodeURIComponent(c.req.param("organizationId"))}`;
  if (!user) return c.redirect(`/sign-in?returnTo=${encodeURIComponent(returnTo)}`);
  return c.html(renderOrganizationPage(user, c.req.param("organizationId")));
});
app.get("/accept-invitation/:invitationId", (c) => {
  const invitationId = c.req.param("invitationId");
  const user = c.get("user");
  const returnTo = `/accept-invitation/${encodeURIComponent(invitationId)}`;
  if (!user) return c.redirect(`/sign-in?returnTo=${encodeURIComponent(returnTo)}`);
  return c.html(renderInvitationPage(user, invitationId));
});

export default app;
