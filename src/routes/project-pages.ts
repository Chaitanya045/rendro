import { Hono } from "hono";
import type { User } from "better-auth/types";

const app = new Hono<{ Variables: { user?: User } }>();

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;",
  })[character] ?? character);
}

function renderProjectPage(user: User, organizationId: string, projectId = ""): string {
  const state = JSON.stringify({ organizationId, projectId }).replace(/</g, "\\u003c");
  return `<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Projects — Rendro</title><style>
:root{color-scheme:light;--page:#fafafa;--surface:#fff;--surface-2:#f4f4f5;--text:#18181b;--muted:#71717a;--border:#e4e4e7;--accent:#f97316;--accent-hover:#fdba74;--danger:#b91c1c;--focus:rgba(249,115,22,.2);--shadow:0 16px 45px rgba(24,24,27,.07)}html.dark{color-scheme:dark;--page:#09090b;--surface:#18181b;--surface-2:#27272a;--text:#fafafa;--muted:#a1a1aa;--border:#3f3f46;--accent:#fb923c;--accent-hover:#fdba74;--danger:#fca5a5;--focus:rgba(251,146,60,.22);--shadow:0 18px 48px rgba(0,0,0,.34)}*{box-sizing:border-box}body{margin:0;background:var(--page);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;-webkit-font-smoothing:antialiased}button,input{font:inherit}a{color:inherit}.topbar{height:64px;border-bottom:1px solid var(--border);background:var(--surface);display:flex;align-items:center;justify-content:space-between;padding:0 clamp(20px,4vw,52px)}.brand{font-size:20px;font-weight:760;letter-spacing:-.04em;text-decoration:none}.brand i{color:var(--accent);font-style:normal}.top-actions{display:flex;align-items:center;gap:16px;color:var(--muted);font-size:12px}.top-actions a{font-weight:650}.theme{width:34px;height:34px;border:1px solid var(--border);border-radius:50%;background:var(--surface);color:var(--muted);cursor:pointer}.page{width:min(1040px,calc(100% - 32px));margin:auto;padding:50px 0 72px}.back{display:inline-block;color:var(--muted);font-size:12px;font-weight:650;text-decoration:none;margin-bottom:24px}.head{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:28px}.head h1{font-size:34px;letter-spacing:-.04em;margin:4px 0 7px}.head p{color:var(--muted);font-size:13px;margin:0}.eyebrow{text-transform:uppercase;letter-spacing:.13em;color:var(--accent)!important;font-size:11px!important;font-weight:750}.layout{display:grid;grid-template-columns:minmax(0,1fr) 320px;gap:24px}.panel{background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow)}.panel-head{padding:19px 21px;border-bottom:1px solid var(--border)}.panel-head h2{font-size:15px;margin:0}.panel-head p{font-size:11px;color:var(--muted);margin:4px 0 0}.list{display:grid}.project{display:grid;grid-template-columns:40px minmax(0,1fr) auto;align-items:center;gap:13px;padding:17px 21px;border-bottom:1px solid var(--border);cursor:pointer}.project:last-child{border-bottom:0}.project:hover{background:var(--surface-2)}.project-icon{width:40px;height:40px;border-radius:8px;background:color-mix(in srgb,var(--accent) 13%,var(--surface));color:var(--accent);display:grid;place-items:center;font-weight:800}.project strong,.project span{display:block}.project strong{font-size:13px}.project span{font-size:11px;color:var(--muted);margin-top:3px}.form-panel{padding:21px;align-self:start}.form-panel h2{font-size:15px;margin:0}.form-panel>p{font-size:12px;line-height:1.5;color:var(--muted);margin:7px 0 19px}.field{display:block;margin-bottom:14px}.field span{display:block;font-size:11px;font-weight:650;margin-bottom:6px}.field input{width:100%;height:40px;border:1px solid var(--border);border-radius:7px;background:var(--surface);color:var(--text);padding:0 10px;outline:none}.field input:focus{border-color:var(--accent);box-shadow:0 0 0 3px var(--focus)}.button{height:40px;border:1px solid var(--border);border-radius:8px;background:var(--surface);color:var(--text);padding:0 14px;font-weight:650;font-size:12px;cursor:pointer;text-decoration:none;display:inline-flex;align-items:center;justify-content:center}.button.primary{background:var(--accent);border-color:var(--accent);color:#18181b}.button.primary:hover{background:var(--accent-hover)}.form-panel .button{width:100%}.error{font-size:11px;color:var(--danger);line-height:1.45}.empty,.loading{padding:48px 22px;text-align:center;color:var(--muted);font-size:12px}.detail{padding:24px}.meta{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin:20px 0}.meta div{border:1px solid var(--border);border-radius:8px;padding:13px}.meta strong,.meta span{display:block}.meta strong{font-size:12px}.meta span{font-size:10px;color:var(--muted);margin-top:4px}.notice{border:1px dashed var(--border);border-radius:9px;padding:20px;color:var(--muted);font-size:12px;line-height:1.55}.code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--text);background:var(--surface-2);padding:2px 5px;border-radius:4px}
@media(max-width:760px){.layout{grid-template-columns:1fr}.form-panel{order:-1}.head{align-items:flex-start;flex-direction:column}.top-actions span{display:none}.meta{grid-template-columns:1fr}.page{padding-top:30px}}
</style></head><body><header class="topbar"><a class="brand" href="/">Rendro<i>.</i></a><div class="top-actions"><span>${escapeHtml(user.email)}</span><a href="/organizations">Organizations</a><a href="/api/auth/sign-out">Sign out</a><button class="theme" id="theme-toggle" aria-label="Switch color theme">◐</button></div></header><main class="page" id="app"><div class="loading">Loading projects…</div></main><script>window.__PROJECT_STATE__=${state};</script><script>
(function(){"use strict";var state=window.__PROJECT_STATE__;var mount=document.getElementById("app");var root=document.documentElement;var media=matchMedia("(prefers-color-scheme: dark)");function theme(){var saved=localStorage.getItem("commentor-theme");return saved==="dark"||saved==="light"?saved:(media.matches?"dark":"light");}function apply(){root.classList.toggle("dark",theme()==="dark");}apply();media.addEventListener("change",apply);document.getElementById("theme-toggle").addEventListener("click",function(){localStorage.setItem("commentor-theme",theme()==="dark"?"light":"dark");apply();});function el(tag,className,text){var node=document.createElement(tag);if(className)node.className=className;if(text!==undefined)node.textContent=text;return node;}async function request(path,options){var response=await fetch(path,Object.assign({headers:{Accept:"application/json","Content-Type":"application/json"}},options||{}));var data=null;try{data=await response.json();}catch(_error){}if(!response.ok)throw new Error(data&&(data.error||data.message)||"Request failed.");return data;}function slugify(value){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"").slice(0,48);}var base="/organizations/"+encodeURIComponent(state.organizationId)+"/projects";
async function renderList(){var data=await request("/api/rendro/projects?organizationId="+encodeURIComponent(state.organizationId));mount.innerHTML='<a class="back" href="/organizations/'+encodeURIComponent(state.organizationId)+'">← Organization settings</a><div class="head"><div><p class="eyebrow">Documentation delivery</p><h1>Projects</h1><p>Each project owns an immutable deployment history and its publications.</p></div></div><div class="layout"><section class="panel"><div class="panel-head"><h2>Organization projects</h2><p>Select a project to view deployments and publication settings.</p></div><div class="list" id="project-list"></div></section><aside class="panel form-panel"><h2>Create project</h2><p>Use one project per independently deployed documentation set.</p><form id="project-form"><label class="field"><span>Name</span><input name="name" required maxlength="80"></label><label class="field"><span>Slug</span><input name="slug" required maxlength="48" pattern="[a-z0-9]+(?:-[a-z0-9]+)*"></label><button class="button primary" type="submit">Create project</button><p class="error" id="project-error"></p></form></aside></div>';var list=document.getElementById("project-list");if(!data.projects.length)list.append(el("div","empty","No projects yet. Create the first project for this organization."));data.projects.forEach(function(project){var row=el("div","project");row.tabIndex=0;row.append(el("span","project-icon",project.name.slice(0,2).toUpperCase()));var copy=el("span");copy.append(el("strong","",project.name),el("span","",project.slug));row.append(copy,el("span","","→"));row.addEventListener("click",function(){location.assign(base+"/"+encodeURIComponent(project._id));});row.addEventListener("keydown",function(event){if(event.key==="Enter")row.click();});list.append(row);});var form=document.getElementById("project-form"),name=form.elements.namedItem("name"),slug=form.elements.namedItem("slug"),touched=false;slug.addEventListener("input",function(){touched=true;});name.addEventListener("input",function(){if(!touched)slug.value=slugify(name.value);});form.addEventListener("submit",async function(event){event.preventDefault();var button=form.querySelector("button"),error=document.getElementById("project-error");button.disabled=true;error.textContent="";try{var result=await request("/api/rendro/projects",{method:"POST",body:JSON.stringify({organizationId:state.organizationId,name:name.value,slug:slug.value})});location.assign(base+"/"+encodeURIComponent(result.project._id));}catch(reason){error.textContent=reason.message;button.disabled=false;}});}
async function renderDetail(){var query="?organizationId="+encodeURIComponent(state.organizationId)+"&projectId="+encodeURIComponent(state.projectId);var data=await request("/api/rendro/projects/get"+query),project=data.project;mount.innerHTML='<a class="back" href="'+base+'">← All projects</a><div class="head"><div><p class="eyebrow">Project</p><h1 id="name"></h1><p id="slug"></p></div><button class="button primary" id="publish-help">Publish from CLI</button></div><section class="panel detail"><div class="meta"><div><strong>Project ID</strong><span id="project-id"></span></div><div><strong>Active deployment</strong><span id="active"></span></div><div><strong>Created</strong><span id="created"></span></div></div><div class="notice">Deployments are immutable. Run <span class="code">rendro push --project '+project.slug+'</span> to stage, upload, and atomically activate a new deployment. Interrupted uploads never replace the active version.</div></section>';document.getElementById("name").textContent=project.name;document.getElementById("slug").textContent=project.slug;document.getElementById("project-id").textContent=project._id;document.getElementById("active").textContent=project.activeDeploymentId||"None yet";document.getElementById("created").textContent=new Date(project.createdAt).toLocaleDateString();document.getElementById("publish-help").addEventListener("click",function(){navigator.clipboard.writeText("rendro push --project "+project.slug);this.textContent="Command copied";});}
async function loadDeployments(){if(!state.projectId)return;var query="?organizationId="+encodeURIComponent(state.organizationId)+"&projectId="+encodeURIComponent(state.projectId),data=await request("/api/rendro/deployments"+query),section=el("section","panel detail");section.style.marginTop="20px";section.append(el("h2","","Deployment history"));if(!data.deployments.length){section.append(el("p","empty","No deployments yet."));}data.deployments.forEach(function(deployment){var row=el("div","meta");var status=el("div");status.append(el("strong","",deployment.status),el("span","",new Date(deployment.createdAt).toLocaleString()));var source=deployment.provenance&&deployment.provenance.source||{};var provenance=el("div");provenance.append(el("strong","","Source"),el("span","",source.commit?source.commit.slice(0,12)+(source.dirty?" · dirty":""):source.localPathLabel||"Local"));var files=el("div");files.append(el("strong","",String(deployment.fileCount)+" files"),el("span","",Math.round(deployment.byteCount/1024)+" KB"));row.append(status,provenance,files);section.append(row);});mount.append(section);}
function fixPushCommand(){var button=document.getElementById("publish-help");if(!button)return;var docs=el("a","button","Browse docs");docs.href="/organizations/"+encodeURIComponent(state.organizationId)+"/projects/"+encodeURIComponent(state.projectId)+"/docs";var shares=el("a","button","Private shares");shares.href="/organizations/"+encodeURIComponent(state.organizationId)+"/projects/"+encodeURIComponent(state.projectId)+"/shares";var publications=el("a","button","Publications");publications.href="/organizations/"+encodeURIComponent(state.organizationId)+"/projects/"+encodeURIComponent(state.projectId)+"/publications";button.before(docs,shares,publications);var command="rendro push --organization "+state.organizationId+" --project "+state.projectId;var notice=document.querySelector(".notice");if(notice)notice.textContent="Deployments are immutable. Run "+command+" to stage, upload, and atomically activate a new deployment. Interrupted uploads never replace the active version.";button.onclick=function(){navigator.clipboard.writeText(command);button.textContent="Command copied";};}
(state.projectId?renderDetail():renderList()).then(function(){fixPushCommand();return loadDeployments();}).catch(function(error){mount.innerHTML='<div class="panel empty"><strong>Unable to load projects.</strong><p class="error"></p><a class="button" href="'+base+'">Try again</a></div>';mount.querySelector("p").textContent=error.message;});})();
</script></body></html>`;
}

app.get("/organizations/:organizationId/projects", (c) => {
  const organizationId = c.req.param("organizationId");
  const user = c.get("user");
  const returnTo = `/organizations/${encodeURIComponent(organizationId)}/projects`;
  if (!user) return c.redirect(`/sign-in?returnTo=${encodeURIComponent(returnTo)}`);
  return c.html(renderProjectPage(user, organizationId));
});
app.get("/organizations/:organizationId/projects/:projectId", (c) => {
  const organizationId = c.req.param("organizationId");
  const projectId = c.req.param("projectId");
  const user = c.get("user");
  const returnTo = `/organizations/${encodeURIComponent(organizationId)}/projects/${encodeURIComponent(projectId)}`;
  if (!user) return c.redirect(`/sign-in?returnTo=${encodeURIComponent(returnTo)}`);
  return c.html(renderProjectPage(user, organizationId, projectId));
});

export default app;
